"""
Setup script for split-utils 
"""

from setuptools import setup

setup(
    name="split-utils",
    version="0.1",
    description="Utilities and visualizations for split mongo courses"
    install_requires=["distribute"],
    requires=[],
)

